import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    sectionContainer: {
        
    },

    button: {
        maxWidth: 200,
        marginRight: 15,
        marginBottom: 20
    }
});

export default styles;
